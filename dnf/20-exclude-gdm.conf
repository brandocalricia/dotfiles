# greetd is the display manager. gdm stays installed, but dnf must not
# upgrade or reinstall it: that once took over login and locked the user out.
# Plain `dnf upgrade` honors this. Override only on purpose:
#   dnf --disable-excludes=main upgrade
[main]
excludepkgs=gdm
