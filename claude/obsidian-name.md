# Name: Obsidian, not "the brain"

Decided 2026-08-28 (user, Mac school laptop): **always call the vault Obsidian.** Never say "the brain" in conversation, new notes, or new commands. Easier that way.

Slash commands: `/obsidian`, `/obsidian-note`, `/obsidian-audit`, `/obsidian-fix`. Old `/brain*` names are aliases.

On-disk path stays `~/Documents/Brain` (Syncthing folder id `brain` on all three machines). Do not rename that folder from one host — fedora and brandon-fedora still use it.

Live vault: `~/Documents/Brain`. Stale Mint-era copy on this Mac: `~/Brain` — do not point the Obsidian app at it, do not delete it unless asked.
