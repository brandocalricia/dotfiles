# Name: Obsidian, not "the brain"

Always call the vault **Obsidian.** Never say "the brain" in conversation or new notes.

On-disk path stays `~/Documents/Brain` (Syncthing folder id `brain` on all three machines). Do not rename that folder.

It is a **manual** notebook. Agents read it only when asked, and write only when asked.
