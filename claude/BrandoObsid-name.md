# Name: BrandoObsid

Always call the vault **BrandoObsid.** Never say "the brain". Never call the vault "Obsidian" — Obsidian is the app.

On-disk path: `~/Documents/BrandoObsid` (symlink into `~/Dropbox/BrandoObsid` on fedora). Dropbox is the only sync. Do not share this folder with Syncthing.

The in-vault `.obsidian` directory stays named `.obsidian`. That is the app's required config folder, not the vault name.

It is a **manual** notebook. Agents read it only when asked, and write only when asked.
