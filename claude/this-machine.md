# This machine — never guess

Three machines:

- `fedora` = **laptop** (Framework 13, Ryzen AI 7 350, Fedora 44 + Hyprland) — school
- `brandon-fedora` = **desktop** (battery-less, dual monitor DP-1 + HDMI-A-1, Fedora 44 + Hyprland) — home
- `Brandons-MacBook-Air-2` = **Mac school laptop** — school. Added 2026-08-28.

Trust `hostname -s`. The vault is shared via Syncthing; never infer which machine you are on from a shared note.

Name trap: laptop restic bucket is `brandon-fedora-home` (host `fedora`). Desktop bucket is `brandon-desktop-home` (host `brandon-fedora`). **Mac: no restic, no B2.**

Call the vault **Obsidian**, never "the brain". Path stays `~/Documents/Brain`. It is a manual notebook — do not write to it unless asked.
